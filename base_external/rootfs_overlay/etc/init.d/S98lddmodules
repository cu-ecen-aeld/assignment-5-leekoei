#!/bin/sh
case "$1" in
    start)
        echo "Load ldd drivers..."
        cd /usr/bin
        scull_load
        module_load faulty
        aesdchar_load
        modprobe hello
        ;;
    stop)
        echo "Unload ldd modules..."
        rmmod aesdchar
        rmmod hello
        cd /usr/bin
        aesdchar_unload
        module_unload faulty
        scull_unload
        ;;
    *)
        echo "Usage: $0 {start|stop}"
        exit 1
        ;;
esac